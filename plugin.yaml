plugins:
  awssdk:
    executor: central_deployment_agent
    package_name: cloudify-boto3-plugin
    package_version: '1.0'
    source: https://github.com/cloudify-incubator/cloudify-boto3-plugin/archive/dev.zip


data_types:
  cloudify.datatypes.aws.ConnectionConfig:
    properties:
      aws_access_key_id:
        description: >
          The ID of your AWS ACCESS KEY ID.
        type: string
        required: false
      aws_secret_access_key:
        description: >
          The ID of your AWS SECRET ACCESS KEY.
        type: string
        required: false
      region_name:
        description: >
          The server region name, such as us-east-1.
          (Not us-east-1b, which is an availability zone, or
          US East, which is a Region.)
        type: string
        required: false

  cloudify.datatypes.aws.dynamodb.Table.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.iam.Group.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.iam.AccessKey.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.iam.LoginProfile.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.iam.User.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.iam.Role.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.iam.Policy.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.lambda.Function.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.lambda.Invoke.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.lambda.Permission.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.Instance.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.InstanceReadReplica.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.SubnetGroup.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.OptionGroup.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.Option.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.ParameterGroup.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.rds.Parameter.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.route53.HostedZone.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.route53.RecordSet.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.LoadBalancer.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.Classic.LoadBalancer.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.Classic.HealthCheck.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.Classic.Listener.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.Classic.Policy.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.Listener.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.Rule.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.elb.TargetGroup.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.SQS.Queue.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.SNS.Topic.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.SNS.Subscription.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.s3.BucketPolicy.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.s3.Bucket.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.ec2.NATGateway.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.autoscaling.Group.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.autoscaling.LaunchConfiguration.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.autoscaling.Policy.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.autoscaling.LifecycleHook.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.cloudwatch.Alarm.config:
    properties:
      kwargs:
        default: {}


  cloudify.datatypes.aws.cloudwatch.Rule.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.cloudwatch.Event.config:
    properties:
      kwargs:
        default: {}

  cloudify.datatypes.aws.cloudwatch.Target.config:
    properties:
      kwargs:
        default: {}

dsl_definitions:
  use_external_resource_desc: &use_external_resource_desc >
    Indicate whether the resource exists or if Cloudify should create the resource,
    true if you are bringing an existing resource, false if you want cloudify to create it.

  resource_id_desc: &resource_id_desc >
    The AWS resource ID of the external resource, if
    use_external_resource is true. Otherwise it is an empty string.


node_types:

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/dynamodb.html#DynamoDB.Client.create_table
  cloudify.nodes.aws.dynamodb.Table:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.dynamodb.Table.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.dynamodb.resources.table.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.dynamodb.resources.table.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_group
  cloudify.nodes.aws.iam.Group:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.iam.Group.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.iam.resources.group.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.iam.resources.group.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_access_key
  cloudify.nodes.aws.iam.AccessKey:
    derived_from: cloudify.nodes.Root
    properties:
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.iam.AccessKey.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        configure:
          implementation: awssdk.cloudify_boto3.iam.resources.access_key.configure
          inputs:
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_login_profile
  cloudify.nodes.aws.iam.LoginProfile:
    derived_from: cloudify.nodes.Root
    properties:
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.iam.LoginProfile.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        configure:
          implementation: awssdk.cloudify_boto3.iam.resources.login_profile.configure
          inputs:
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_user
  cloudify.nodes.aws.iam.User:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.iam.User.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.iam.resources.user.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.iam.resources.user.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_role
  cloudify.nodes.aws.iam.Role:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.iam.Role.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.iam.resources.role.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.iam.resources.role.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_policy
  cloudify.nodes.aws.iam.Policy:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.iam.Policy.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.iam.resources.policy.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.iam.resources.policy.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/lambda.html#Lambda.Client.create_function
  cloudify.nodes.aws.lambda.Function:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.lambda.Function.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.lambda_serverless.resources.function.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.lambda_serverless.resources.function.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/lambda.html#Lambda.Client.invoke
  # Stores invocation output in runtime property "output".
  cloudify.nodes.aws.lambda.Invoke:
    derived_from: cloudify.nodes.Root
    properties:
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.lambda.Invoke.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        configure:
          implementation: awssdk.cloudify_boto3.lambda_serverless.resources.invoke.configure
          inputs:
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/lambda.html#Lambda.Client.add_permission
  # Stores method output in runtime property "output".
  cloudify.nodes.aws.lambda.Permission:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.lambda.Permission.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.lambda_serverless.resources.permission.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.lambda_serverless.resources.permission.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.lambda_serverless.resources.permission.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  cloudify.nodes.aws.rds.Instance:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.Instance.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.rds.resources.instance.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.rds.resources.instance.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.rds.resources.instance.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance_read_replica
  cloudify.nodes.aws.rds.InstanceReadReplica:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.InstanceReadReplica.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.rds.resources.instance_read_replica.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.rds.resources.instance_read_replica.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.rds.resources.instance_read_replica.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_subnet_group
  cloudify.nodes.aws.rds.SubnetGroup:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.SubnetGroup.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.rds.resources.subnet_group.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.rds.resources.subnet_group.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.rds.resources.subnet_group.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_option_group
  cloudify.nodes.aws.rds.OptionGroup:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.OptionGroup.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.rds.resources.option_group.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.rds.resources.option_group.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Stores prepared Option in runtime property "resource_config"
  cloudify.nodes.aws.rds.Option:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.Option.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        configure:
          implementation: awssdk.cloudify_boto3.rds.resources.option.configure
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_parameter_group
  cloudify.nodes.aws.rds.ParameterGroup:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.ParameterGroup.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.rds.resources.parameter_group.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.rds.resources.parameter_group.configure
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.rds.resources.parameter_group.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Stores prepared Parameter in runtime property "resource_config"
  cloudify.nodes.aws.rds.Parameter:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.rds.Parameter.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        configure:
          implementation: awssdk.cloudify_boto3.rds.resources.parameter.configure
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/route53.html#Route53.Client.create_hosted_zone
  cloudify.nodes.aws.route53.HostedZone:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.route53.HostedZone.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.route53.resources.hosted_zone.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.route53.resources.hosted_zone.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.route53.resources.hosted_zone.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            force_delete:
              description: >
                Hosted Zones can only be deleted if all Resource Sets (except for the
                default ones) are already removed. If this is set to `true`, the operation
                will attempt to delete all Resource Sets within the Hosted Zone before
                deleting the Hosted Zone itself.
              required: true
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/route53.html#Route53.Client.change_resource_record_sets
  cloudify.nodes.aws.route53.RecordSet:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.route53.RecordSet.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.route53.resources.record_set.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.route53.resources.record_set.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.route53.resources.record_set.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # Reference: https://boto3.readthedocs.io/en/latest/reference/services/sqs.html
  cloudify.nodes.aws.SQS.Queue:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.SQS.Queue.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.sqs.resources.queue.prepare

          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.sqs.resources.queue.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.sqs.resources.queue.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  # https://boto3.readthedocs.io/en/latest/reference/services/sns.html
  cloudify.nodes.aws.SNS.Topic:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.SNS.Topic.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.sns.resources.topic.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.sns.resources.topic.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.sns.resources.topic.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.SNS.Subscription:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.SNS.Subscription.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.sns.resources.subscription.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.sns.resources.subscription.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        start:
          implementation: awssdk.cloudify_boto3.sns.resources.subscription.start
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.sns.resources.subscription.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}


  cloudify.nodes.aws.elb.LoadBalancer:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.LoadBalancer.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.load_balancer.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.load_balancer.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        start:
          implementation: awssdk.cloudify_boto3.elb.resources.load_balancer.modify
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.load_balancer.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Classic.LoadBalancer:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.Classic.LoadBalancer.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.load_balancer.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.load_balancer.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        start:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.load_balancer.start
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.load_balancer.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Classic.HealthCheck:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.Classic.HealthCheck.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.health_check.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.health_check.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Listener:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.Listener.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.listener.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.listener.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.listener.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Classic.Listener:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.Classic.Listener.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.listener.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.listener.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.listener.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Rule:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.Rule.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.rule.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.rule.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.rule.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.TargetGroup:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.TargetGroup.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.target_group.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.target_group.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        start:
          implementation: awssdk.cloudify_boto3.elb.resources.target_group.modify
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.target_group.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Classic.Policy:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.elb.Classic.Policy.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.policy.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.policy.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.policy.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.elb.Classic.Policy.Stickiness:
    derived_from: cloudify.nodes.aws.elb.Classic.Policy
    interfaces:
      cloudify.interfaces.lifecycle:
        configure:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.policy.create_sticky
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        start:
          implementation: awssdk.cloudify_boto3.elb.resources.classic.policy.start_sticky
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.s3.Bucket:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.s3.Bucket.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.s3.resources.bucket.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.s3.resources.bucket.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.s3.resources.bucket.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.s3.BucketPolicy:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.s3.BucketPolicy.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.s3.resources.bucket_policy.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.s3.resources.bucket_policy.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.s3.resources.bucket_policy.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.ec2.NATGateway:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.ec2.NATGateway.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.ec2.resources.nat_gateway.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.ec2.resources.nat_gateway.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.ec2.resources.nat_gateway.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.autoscaling.Group:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.autoscaling.Group.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.autoscaling_group.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.autoscaling_group.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.autoscaling_group.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.autoscaling.LaunchConfiguration:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.autoscaling.LaunchConfiguration.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.launch_configuration.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.launch_configuration.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.launch_configuration.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.autoscaling.Policy:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.autoscaling.Policy.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.policy.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.policy.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.policy.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.autoscaling.LifecycleHook:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.autoscaling.LifecycleHook.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.lifecycle_hook.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.lifecycle_hook.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.autoscaling.resources.lifecycle_hook.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.cloudwatch.Alarm:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.cloudwatch.Alarm.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.alarm.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.alarm.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.alarm.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.cloudwatch.Rule:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.cloudwatch.Rule.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.rule.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.rule.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.rule.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.cloudwatch.Event:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.cloudwatch.Event.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.event.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.event.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

  cloudify.nodes.aws.cloudwatch.Target:
    derived_from: cloudify.nodes.Root
    properties:
      use_external_resource:
        description: *use_external_resource_desc
        type: boolean
        default: false
      resource_id:
        description: *resource_id_desc
        type: string
        default: ''
      client_config:
        description: >
          A dictionary of values to pass to authenticate with the AWS API.
        type: cloudify.datatypes.aws.ConnectionConfig
        required: false
      resource_config:
        description: >
          Configuration key-value data to be passed as-is to the corresponding
          Boto3 method. Key names must match the case that Boto3 requires.
        type: cloudify.datatypes.aws.cloudwatch.Target.config
        required: false
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.target.prepare
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        configure:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.target.create
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        delete:
          implementation: awssdk.cloudify_boto3.cloudwatch.resources.target.delete
          inputs:
            aws_resource_id:
              description: >
                This overrides the resource_id property (useful for setting the
                resource ID of a node instance at runtime).
              type: string
              required: false
              default: ~
            runtime_properties:
              description: >
                This overrides any runtime property at runtime. This is a key-value
                pair / dictionary that will be passed, as-is, to the runtime properties
                of the running instance.
              required: false
              default: ~
            force_operation:
              description: >
                Forces the current operation to be executed regardless
                if the "use_external_resource" property is set or not.
              required: false
              default: false
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}

relationships:

  cloudify.relationships.aws.connected_to:
    derived_from: cloudify.relationships.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure:
          implementation: ~
          inputs:
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        postconfigure:
          implementation: ~
          inputs:
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        establish:
          implementation: ~
          inputs:
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}
        unlink:
          implementation: ~
          inputs:
            resource_config:
              description: >
                Configuration key-value data to be passed as-is to the corresponding
                Boto3 method. Key names must match the case that Boto3 requires.
              required: false
              default: {}



  ####
  # AWS Identity & Access Management
  ####

  ##
  # IAM USER
  ##
  # IAM GROUP: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.add_user_to_group
  # IAM POLICY: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.attach_user_policy
  # IAM LOGIN PROFILE: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_login_profile
  # IAM ACCESS KEY: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_access_key
  cloudify.relationships.aws.iam.user.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.iam.resources.user.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.iam.resources.user.detach_from }

  ##
  # IAM GROUP
  ##
  # IAM USER: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.add_user_to_group
  # IAM POLICY: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.attach_group_policy
  cloudify.relationships.aws.iam.group.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.iam.resources.group.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.iam.resources.group.detach_from }

  ##
  # IAM ACCESS KEY
  ##
  # IAM USER: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_access_key
  cloudify.relationships.aws.iam.access_key.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.iam.resources.access_key.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.iam.resources.access_key.detach_from }

  ##
  # IAM LOGIN PROFILE
  ##
  # IAM USER: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.create_login_profile
  cloudify.relationships.aws.iam.login_profile.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.iam.resources.login_profile.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.iam.resources.login_profile.detach_from }

  ##
  # IAM ROLE
  ##
  # IAM POLICY: https://boto3.readthedocs.io/en/latest/reference/services/iam.html#IAM.Client.attach_role_policy
  cloudify.relationships.aws.iam.role.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.iam.resources.role.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.iam.resources.role.detach_from }


  ####
  # AWS Lambda
  ####

  ##
  # LAMBDA INVOKE
  ##
  # LAMBDA FUNCTION: https://boto3.readthedocs.io/en/latest/reference/services/lambda.html#Lambda.Client.invoke
  cloudify.relationships.aws.lambda.invoke.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.lambda_serverless.resources.invoke.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.lambda_serverless.resources.invoke.detach_from }

  ##
  # LAMBDA PERMISSION
  ##
  # LAMBDA FUNCTION: https://boto3.readthedocs.io/en/latest/reference/services/lambda.html#Lambda.Client.add_permission
  cloudify.relationships.aws.lambda.permission.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure: { implementation: awssdk.cloudify_boto3.lambda_serverless.resources.permission.prepare_assoc }
        unlink: { implementation: awssdk.cloudify_boto3.lambda_serverless.resources.permission.detach_from }


  ####
  # AWS Relational Database Service
  ####

  ##
  # RDS INSTANCE READ REPLICA
  ##
  # RDS INSTANCE: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance_read_replica
  # RDS PARAMETER GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance_read_replica
  # RDS OPTION GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance_read_replica
  # IAM ROLE: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  cloudify.relationships.aws.rds.instance_read_replica.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure:
          implementation: awssdk.cloudify_boto3.rds.resources.instance_read_replica.prepare_assoc
          inputs:
            iam_role_type_key:
              description: >
                If the relationship target is an IAM Role, it's necessary to give more context in order to
                match the Role with the correct mapping to the relationship source since this type accepts
                more than 1 type of Role. For instance, the RDS.Client.create_db_instance() method can
                take both "DomainIAMRoleName" and "MonitoringRoleArn" keys. Consider this input as the
                destination function keyword for the Role's ID or ARN. This is a required input if the
                relationship target is of type IAM Role.
              type: string
              default: MonitoringRoleArn
              required: false
            iam_role_id_key:
              description: >
                If the relationship target is an IAM Role, it's necessary to give more context in order to
                match the Role with the correct mapping to the relationship source since this type accepts
                more than 1 type of Role. The value for this input should be the key name of a runtime
                property (of the target) which will be subsequently placed as the value for the destination
                function keyword from the "iam_role_type_key" input. This is a required input if the
                relationship target is of type IAM Role.
              type: string
              default: aws_resource_arn
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.instance_read_replica.detach_from }

  ##
  # RDS INSTANCE
  ##
  # RDS SUBNET GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  # RDS PARAMETER GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  # RDS OPTION GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  # IAM ROLE: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  # EC2 SECURITY GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_instance
  cloudify.relationships.aws.rds.instance.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure:
          implementation: awssdk.cloudify_boto3.rds.resources.instance.prepare_assoc
          inputs:
            iam_role_type_key:
              description: >
                If the relationship target is an IAM Role, it's necessary to give more context in order to
                match the Role with the correct mapping to the relationship source since this type accepts
                more than 1 type of Role. For instance, the RDS.Client.create_db_instance() method can
                take both "DomainIAMRoleName" and "MonitoringRoleArn" keys. Consider this input as the
                destination function keyword for the Role's ID or ARN. This is a required input if the
                relationship target is of type IAM Role.
              type: string
              default: ~
              required: false
            iam_role_id_key:
              description: >
                If the relationship target is an IAM Role, it's necessary to give more context in order to
                match the Role with the correct mapping to the relationship source since this type accepts
                more than 1 type of Role. The value for this input should be the key name of a runtime
                property (of the target) which will be subsequently placed as the value for the destination
                function keyword from the "iam_role_type_key" input. This is a required input if the
                relationship target is of type IAM Role.
              type: string
              default: aws_resource_arn
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.instance.detach_from }

  ##
  # RDS SUBNET GROUP
  ##
  # EC2 VPC SUBNET: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.create_db_subnet_group
  cloudify.relationships.aws.rds.subnet_group.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure: { implementation: awssdk.cloudify_boto3.rds.resources.subnet_group.prepare_assoc }
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.subnet_group.detach_from }

  ##
  # RDS PARAMETER GROUP
  ##
  # RDS PARAMETER: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.modify_db_parameter_group
  cloudify.relationships.aws.rds.parameter_group.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.rds.resources.parameter_group.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.parameter_group.detach_from }

  ##
  # RDS PARAMETER
  ##
  # RDS PARAMETER GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.modify_db_parameter_group
  cloudify.relationships.aws.rds.parameter.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.rds.resources.parameter.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.parameter.detach_from }

  ##
  # RDS OPTION GROUP
  ##
  # RDS OPTION: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.modify_db_option_group
  cloudify.relationships.aws.rds.option_group.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.rds.resources.option_group.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.option_group.detach_from }

  ##
  # RDS OPTION
  ##
  # RDS OPTION GROUP: https://boto3.readthedocs.io/en/latest/reference/services/rds.html#RDS.Client.modify_db_option_group
  cloudify.relationships.aws.rds.option.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.rds.resources.option.attach_to }
        unlink: { implementation: awssdk.cloudify_boto3.rds.resources.option.detach_from }


  ####
  # AWS Route53 DNS Service
  ####

  ##
  # ROUTE53 HOSTED ZONE
  ##
  # EC2 VPC: https://boto3.readthedocs.io/en/latest/reference/services/route53.html#Route53.Client.create_hosted_zone
  cloudify.relationships.aws.route53.hosted_zone.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure: { implementation: awssdk.cloudify_boto3.route53.resources.hosted_zone.prepare_assoc }
        unlink: { implementation: awssdk.cloudify_boto3.route53.resources.hosted_zone.detach_from }

  ##
  # ROUTE53 RESOURCE RECORD SET
  ##
  # ROUTE53 HOSTED ZONE: https://boto3.readthedocs.io/en/latest/reference/services/route53.html#Route53.Client.change_resource_record_sets
  cloudify.relationships.aws.route53.record_set.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        preconfigure: { implementation: awssdk.cloudify_boto3.route53.resources.record_set.prepare_assoc }
        unlink: { implementation: awssdk.cloudify_boto3.route53.resources.record_set.detach_from }

  ##
  # Instance to LB
  ##
  cloudify.relationships.aws.elb.instance.connected_to:
    derived_from: cloudify.relationships.aws.connected_to
    source_interfaces:
      cloudify.interfaces.relationship_lifecycle:
        establish: { implementation: awssdk.cloudify_boto3.elb.resources.classic.load_balancer.assoc }
        unlink: { implementation: awssdk.cloudify_boto3.elb.resources.classic.load_balancer.disassoc }
